apiVersion: apps/v1
kind: Deployment
metadata:
  name: car-value-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: car-value-service
  template:
    metadata:
      labels:
        app: car-value-service
    spec:
      containers:
      - name: car-value-service
        image: 469511944493.dkr.ecr.us-east-1.amazonaws.com/car-value-service:latest
        ports:
        - containerPort: 5001
        resources:
          requests:
            memory: "512Mi"
            cpu: "150m"
          limits:
            memory: "800Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: car-value-service
spec:
  selector:
    app: car-value-service
  ports:
  - protocol: TCP
    port: 5001
    targetPort: 5001
  type: ClusterIP